name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Run one line script
      run: echo hello world

    - name: Run a multi line script
      run: echo Add other actions,echo thanks actions
     
